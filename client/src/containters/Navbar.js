import React from 'react';
import { Link } from "react-router";

export const Navbar = React.createClass({
    render(){
        return(
            <div>
                <ul>
                    
                </ul>
            </div>
        )
    }
})